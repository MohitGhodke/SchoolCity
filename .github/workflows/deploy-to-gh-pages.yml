name: Deploy to GitHub Pages

on:
  push:
    branches: 
      - main
      - copilot/deploy-changes-to-production
  workflow_dispatch:  # Allow manual triggering

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: school-game/package-lock.json
          
      - name: Install dependencies
        run: |
          cd school-game
          npm ci
          
      - name: Build Angular app
        run: |
          cd school-game
          npm run build:prod
          
      - name: Add .nojekyll file
        run: |
          touch school-game/dist/school-game/browser/.nojekyll
          
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./school-game/dist/school-game/browser
          publish_branch: gh-pages
          commit_message: 'Deploy: ${{ github.event.head_commit.message }}'
